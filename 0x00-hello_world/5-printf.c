#include <stdio.h>

/**
 * main - Entery point
 * printf() - use the printf function to display text
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
