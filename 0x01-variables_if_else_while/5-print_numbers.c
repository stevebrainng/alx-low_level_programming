#include <stdio.h>
/**
 * main - Program Entery point
 *
 * Return: 0 on success or of different number on error
 */
int main(void)
{
	char i;

	for (i = '0'; a <= '9'; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
